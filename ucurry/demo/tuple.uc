-- compiler is stores a tuple of string and integer 
(string * int) compiler = ("CS", 107); 

-- The element inside the tuple can be accessed through the . notation, followed by the index. 
println compiler.0; 
println compiler.1;